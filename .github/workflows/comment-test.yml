name: Comment Test
on:
  issue_comment:
    types: [created]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Processing comment: ${{ github.event.comment.body }}"
      - run: echo "Comment by ${{ github.event.comment.user.login }}"
